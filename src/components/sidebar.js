import React from 'react';

// Set the store, reducers and the components
var Sidebar = React.createClass({
  render: function() {
    return(
        <div className="col-md-3" style={{height:'90vh'}} >
            sdfjd      
        </div>
    );
  }
});

export default Sidebar;
